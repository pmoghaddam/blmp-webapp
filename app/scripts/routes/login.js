/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var LoginRouter = Backbone.Router.extend({
        routes: {
        }
    });

    return LoginRouter;
});
