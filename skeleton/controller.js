/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var Controller = Backbone.Controller.extend({
        start: function () {
            // Starting logic of controller
        }
    });

    return Controller;
});
