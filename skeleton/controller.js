/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var BasicController = Backbone.Controller.extend({
        start: function () {
            // Starting logic of controller
        }
    });

    return BasicController;
});
