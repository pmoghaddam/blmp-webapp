/*global define*/

define([
    'jquery',
    'backbone',
    'lib/blpm',
    'q'
], function ($, Backbone, BLPM, Q) {
    'use strict';

    var Service = BLPM.Service.extend({
    });

    return Service;
});
