/*global define*/

define([
    'jquery',
    'backbone',
    'q'
], function ($, Backbone, q) {
    'use strict';

    var BasicService = Backbone.Service.extend({
    });

    return BasicService;
});
